import React from "react";
import AvatarEditor from "react-avatar-editor";

const ImageCropper = ({ width, height, image }) => {
  // return <AvatarEditor width={width} height={height} image={image} />;
};

export default ImageCropper;
